//Project file
import Hero from "../components/shared/Hero";

// -1 If the only thing that exist here is the Hero, then copy and paste the code of the hero here.
// Otherwise this component is useless.
export default function Home() {
  return (
    <div className="home">
      <Hero />
    </div>
  );
}
